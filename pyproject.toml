[project]
name = "solomon"
version = "0.1.0"
description = "Solomon - Personal assistant"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
