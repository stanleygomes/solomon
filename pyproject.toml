[project]
name = "solomon"
version = "0.1.0"
description = "Solomon - Personal assistant"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "textual>=8.2.7",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
    "textual-dev>=1.8.0",
]
